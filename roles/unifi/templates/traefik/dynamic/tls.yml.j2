tls:
  certificates:
    - certFile: /certs/{{ unifi_service_domain }}.crt
      keyFile: /certs/{{ unifi_service_domain }}.pem
  options:
    default:
      minVersion: VersionTLS12
      sniStrict: true
